name: Github actions - Push - Branch Template
run-name: Running - ${{ github.event_name}}
on:
  push:
    branches:
      - develop

jobs:
  on-push:
    runs-on: ubuntu-20.04
    steps:
      - name: Comentário
        run: |
          echo "Comentario PUSH WORKFlOW - branch"
