name: GitHub Actions - Gerenciamento erros
run-name: Running - Gerenciamento erros
on: [push, workflow_dispatch]

jobs:
  job_1_erro:
    runs-on: ubuntu-latest
    steps:
    - name: Comentário
      run: |
        echo Comentario antes do erro
        exit 1
    - name: 
      run: |
        echo Comentario apos o erro