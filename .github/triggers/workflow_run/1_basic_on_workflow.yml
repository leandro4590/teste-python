name: GitHub Actions - Workflow run Basic Template
run-name: Running - Workflow run Basic Template
on:
  workflow_run:
    workflows: [GitHub Actions - Trigger 1]
jobs:
  on-push:
    runs-on: ubuntu-latest
    steps:
      - name: Comentario
        run: |
          echo "TWorkflow que foi trigado"
